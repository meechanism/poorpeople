import React from 'react';

export default ({ children }) => (
  <div className="bg-navy near-white pv5 flex flex-column items-center">
    <h1 className="db f1 display fw1">{children}</h1>
  </div>
);
